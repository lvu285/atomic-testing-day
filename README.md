# atomic-generated-answer-testing-day

A sample project for testing and validating the search use case in latest CDN atomic/headless in dev

## Start

```sh
cp .env.example .env
npm install
npm run dev
```

Windows:

```bat
copy .env.example .env
npm install
npm run dev
```
