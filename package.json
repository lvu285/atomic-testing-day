{
  "name": "atomic-generated-answer-testing-day",
  "description": "A sample project for testing and validating the search use case in @coveo/atomic",
  "type": "module",
  "scripts": {
    "dev": "npx vite",
    "build": "npx vite build"
  },

  "devDependencies": {
    "vite": "6.0.2",
    "fs-extra": "^11.2.0"
  }
}
